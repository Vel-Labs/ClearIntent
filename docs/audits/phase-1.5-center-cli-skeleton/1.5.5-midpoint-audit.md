# Phase 1.5 Center CLI Skeleton Midpoint Audit

## Audit type

- midpoint audit

## Status

- pass

## Scope

This audit covers workstreams `1.5.1` through `1.5.4`: CLI scaffold, command grouping, fixture-backed state/evaluation commands, and output modes.

In scope:

- `packages/center-cli/` scaffold
- command routing for `center status`, `center inspect`, `intent validate`, `intent state`, and `authority evaluate`
- human-readable default output
- deterministic `--json` output
- fixture-backed state and authority evaluation through `packages/core`

Out of scope:

- ENS, 0G, KeeperHub, signer, webhook, OS notification, browser UI, hosted service, and demo integration
- provider adapter directories
- changes to `contracts/`

## Fresh evidence

- `packages/center-cli/src/cli.ts`
- `packages/center-cli/src/commands.ts`
- `packages/center-cli/src/fixtures.ts`
- `packages/center-cli/src/output.ts`
- `tests/center-cli/center-cli.test.ts`

## Audit actions executed

- Inspected CLI routing to confirm it calls `deriveCoreStateSnapshot`, `evaluateCoreAuthority`, and `createContractValidator`.
- Inspected output formatting to confirm human and JSON output are separate render paths over the same `CliCommandResult`.
- Ran focused CLI tests.
- Ran representative CLI smoke commands in human and JSON modes.
- Ran TypeScript checking after the initial implementation slice.

## Inputs reviewed

- `docs/roadmaps/phase-1.5-center-cli-skeleton/IMPLEMENTATION_PLAN.md`
- `packages/core/API.md`
- `packages/core/src/module-api.ts`
- `packages/core/src/state.ts`
- `packages/core/src/status.ts`
- `packages/core/src/verification.ts`
- `contracts/examples/*.json`
- changed CLI source and tests

## Findings

- Critical: none.
- High: none.
- Medium: none.
- Low: Npm's normal `npm run` banner precedes command output unless callers use `npm run --silent`. The CLI JSON renderer itself emits raw JSON with no leading prose. Documentation now directs machine consumers to `npm run --silent clearintent -- <command> --json`.

## Verification evidence

```bash
npm test -- tests/center-cli
```

- Result: passed. `tests/center-cli/center-cli.test.ts` ran 9 tests successfully after the human landing path and fixture metadata update.

```bash
npm run clearintent -- center status
```

- Result: produced human-readable output showing `Status: blocked`, lifecycle `human_approved`, next action `collect_signature`, and missing evidence `signature`.

```bash
npm run clearintent -- center inspect --json
```

- Result: command-level payload was deterministic JSON with `command: "center inspect"` and snapshot issue code `missing_lifecycle_evidence`. Npm banner behavior is documented for silent machine usage.

```bash
npm run clearintent -- authority evaluate --fixture expired --json
```

- Result: command-level JSON reported `deadline_expired` and `deadline_before_created_at` from `evaluateCoreAuthority`.

```bash
npm run clearintent -- module doctor
```

- Result: module doctor reported core as `ready` and future modules as `deferred`.

```bash
npm run typecheck
```

- Result: passed.

## Git disposition

- local only

## Decision

- pass

## Next action

Proceed with `1.5.6` through `1.5.8`: module list/doctor skeleton hardening, docs updates, file-tree refresh, and full verification before closeout.
